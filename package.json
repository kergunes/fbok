{
  "name": "fbok",
  "version": "0.5.3",
  "private": true,
  "description": "Facebook-specific sponsored-post detector for Chromium browsers.",
  "scripts": {
    "test": "node scripts/validate.mjs && node --check src/content.js && node --check popup.js"
  },
  "engines": {
    "node": ">=20"
  }
}
